var x = 123456723;

//convert the number into string
console.log("convert the number into string",x.toString());

//convert the number into exponential
console.log("convert the number into exponential",x.toExponential(4));

//convert the number into specific number
console.log("convert the number into specific numbet",x.toFixed(3));

//convert the number into specific length
console.log("convert the number into specific length",x.toPrecision(4));

//convert the number as number
console.log("convert the number as number",x.valueOf());

